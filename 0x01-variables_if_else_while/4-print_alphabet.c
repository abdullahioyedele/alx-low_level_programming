#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	alpha = 97;

	while (alpha <= 127 && alpha != (101 && 113))
	{
		putchar(alpha);
		alpha++;
	}

		putchar(\n);

		return (0);
}
